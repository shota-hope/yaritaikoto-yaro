# AGENTS.md

このファイルは、このリポジトリで作業するAIエージェント向けの実行ルールです。

## 作業を始める前に

- プロダクト仕様と決定事項は [`docs/product.md`](docs/product.md) を正本とする。
- Issue、ブランチ、PR、レビューの手順は [`docs/development.md`](docs/development.md) に従う。
- `README.md`、対象コード、関連テストを読み、現在の実装を確認してから変更する。
- 作業ツリーに既存の変更がある場合は、それを利用者の作業として保持する。依頼と無関係な変更は編集、削除、整形しない。
- 要件が未決の場合は、`docs/product.md` の「未決事項・レビュー対象」を確認する。未決の案を確定仕様として実装しない。

## 変更の方針

- 新しい機能や利用者体験を作るときは、何を、誰のために、どこまで作るのかを、必ず利用者と議論する。
- 議論した内容を短い計画または仕様として示し、利用者の明示的な確認を得てから実装を始める。提案、推測、未回答の質問を承認として扱わない。
- 確認を得る前に行ってよいのは、現状の調査、選択肢の整理、計画・仕様文書の作成までとする。
- 承認後に目的や対象範囲を変える必要が生じた場合は、実装を進める前に変更点を説明し、改めて確認を得る。
- 依頼された目的を満たす最小の変更にする。
- スマートフォンの縦画面とタップ操作を優先し、横スクロールを発生させない。
- 入力項目と操作を増やす前に、本当に初期版に必要かを `docs/product.md` で確認する。
- 日付の判定にはAsia/Tokyoの暦日を使う。日付計算や分類は、できる限り副作用のない関数として `src/core.ts` に置く。
- 利用者の入力データを壊さない。保存形式を変更する場合は、既存データとの互換性または移行方法を用意する。
- 健康寿命は行動を考えるための目安として扱い、個人の寿命や健康状態を断定する表現を使わない。
- 将来機能を先回りして実装しない。

## コード

- ReactとTypeScriptの既存構成、命名、スタイルに合わせる。
- 型の安全性を保ち、理由のない `any` や型アサーションを増やさない。
- UIから分離できる日付・分類・データ変換ロジックは `src/core.ts` に置く。
- 境界条件やデータ破損につながるロジックには、振る舞いを確認できるテストを追加する。
- 新しい依存パッケージは、既存の依存だけでは目的を達成できない場合に限る。
- コメントにはコードから分からない理由や制約を書く。処理内容をそのまま言い換えるコメントは追加しない。

## 仕様と文書

- 会話やレビューで仕様・方針が決まった場合は、実装と同じ変更で `docs/product.md` の本文と決定履歴を更新する。
- 開発手順を変更する場合は `docs/development.md` を更新する。
- コマンド、構成、利用者向けの挙動が変わる場合は `README.md` も確認する。
- 同じ説明を複数の文書へコピーせず、正本へのリンクを使う。

## 検証

- コードを変更したら、少なくとも `npm test` と `npm run build` を実行する。
- UIを変更したら、スマートフォン幅で対象操作をブラウザ確認する。
- 入力、再読み込み後の保持、編集、達成など、変更に関係する利用者の一連の操作を確認する。
- 実行できなかった確認や残る制約は、完了報告に明記する。

## Gitと完了報告

- `main` に直接実装しない。`docs/development.md` の手順に従って作業ブランチとPRを使う。
- コミットとPRは、変更の目的が分かる小さな単位にする。
- 利用者の明示的な依頼なしに、PRのマージやデプロイをしない。
- コミットやPRには、依頼と無関係な変更を含めない。
- 完了報告では、変更した内容、確認結果、残る課題を簡潔に伝える。
